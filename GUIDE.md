# Research Agent — 사용 가이드

> 학술 글(에세이·과제·논문)을 쓸 때, **AI가 옆에서 도와주는 작업 도구**입니다.
> 처음 쓰는 분도 이 문서만 따라하면 글 한 편을 끝낼 수 있어요.

---

## 🎯 이 시스템이 뭘 해주나요?

글 한 편 쓰는 데 보통 이런 게 필요합니다:

1. 무슨 얘기 할지 줄거리 잡기
2. 인용할 논문 찾기
3. 논문 읽고 분석하기 (어떤 인용을 어디에 쓸지)
4. 본문 쓰기
5. 다듬기·인용 검증·참고문헌 만들기

**이 도구가 도와주는 부분**:
- 줄거리 약점 찾아주기 (논리 구멍·근거 부족·반박 대비 등)
- 논문 자동 검색·요약·분석
- 본문 초안 작성
- 인용 정확성 자동 확인
- 참고문헌 자동 생성

**여러분이 하는 일**:
- 글의 **방향과 핵심 주장** 정하기
- 어떤 논문을 다운로드할지 **결정**
- AI가 만든 결과 **검토·수정**
- 최종 판단

---

## ⚡ 5분 설치 + 시작

### 처음 한 번만

설치는 [README.md](./README.md)를 따라하세요. 끝났다면:

```bash
cd <research-agent 설치 폴더>     # 본인 환경 (예: ~/Documents/research-agent)
claude
```

이제 Claude Code 안에서 한국어로 명령하면 됩니다. **명령어를 외울 필요 없어요** — `"이런거 해줘"` 식으로 자연어로 말해도 알아듣습니다.

---

## 🗺 글 한 편 쓰는 전체 흐름

```
   ① 프로젝트 만들기
        ↓
   ② 줄거리(flow.md) 쓰기 ─┐
        ↓                  │
   ③ 줄거리 분석받기        │ flow 단계
        ↓                  │ (계획 단계)
   ④ 논문 자동 검색         │
        ↓                  │
   ⑤ PDF 다운로드           │
        ↓                  │
   ⑥ 논문 분석 ─────────────┘
        ↓
   ⑦ 초안 작성 ──────────┐
        ↓                │
   ⑧ 분석 + 수정 (반복)   │ output 단계
        ↓                │ (원고 단계)
   ⑨ 마무리 (검증·통합) ──┘
        ↓
      🎉 완성
```

⚠ **단방향 진행**: ⑦ 초안 작성 후엔 flow(계획) 단계로 되돌아가지 않습니다. flow.md는 보관되지만 새로 수정하지 않아요.

---

## 📋 단계별 안내

### ① 프로젝트 만들기

**명령**:
```
"my-essay 프로젝트 만들어줘"
```
(my-essay 자리에 본인 글 이름)

**무엇이 생기나요?**
- `projects/my-essay/` 폴더가 통째로 생김
- 안에 빈 `flow/flow.md` 파일이 준비됨

**다음 할 일**: ② flow.md에 줄거리 쓰기

---

### ② 줄거리(flow.md) 쓰기

**무엇을 하나요?**: 직접 에디터로 `projects/my-essay/flow/flow.md` 열고 글의 **줄거리를 줄글로** 작성합니다. 체크리스트가 아닌 **자유로운 문단**으로:

**좋은 flow.md 예시**:
```markdown
# 본 에세이 주제

이 글은 "AI가 학술 글쓰기를 어떻게 바꾸는가"를 다룬다.
본 에세이는 AI가 보조 도구일 때만 효과적이고, 대체할 때는
원저자의 사고 과정 형성을 방해한다고 주장한다.

## 1. 도입
AI 글쓰기 도구의 부상...

## 2. 기존 입장
...

## 3. 본 에세이의 입장
...

## 4. 예상 반론과 응답
...

## 5. 결론
...
```

**팁**:
- 한 문장으로 핵심 주장 (`본 에세이는 X라고 주장한다`)
- 구조: 문제 → 기존 입장 → 본 입장 → 반박 → 함의

**무엇을 보면 되나요?**: 본인이 작성한 `flow.md` 만 신경쓰면 됩니다.

**다음 할 일**: ③ 분석받기

---

### ③ 줄거리 분석받기

**명령** (둘 다 또는 하나):
```
"레퍼런스 분석해줘"      ← 어떤 논문이 필요한지 찾아줌
"내용 분석해줘"          ← 논리 구멍·약점을 진단해줌
```

**무엇이 생기나요?**

| 파일 | 보세요? | 무엇 |
|------|--------|------|
| `flow/evaluations/evaluation.md` | ✅ 꼭 | **종합 진단** — 어디가 약하고 뭘 보강할지 |
| `work-plan.md` | ✅ 꼭 | **할 일 목록** — 다음에 뭘 해야 할지 카드로 정리 |

**무엇을 확인하나요?**

`evaluation.md`를 열어보면 5축 평가가 보입니다:
- 🟢 충실: 좋음
- 🟡 적정: 통과 가능
- 🟠 보강 필요: 손봐야 함
- 🔴 구조적 결함: 큰 문제
- ⚫ 측정 불가: 데이터 부족

빨강(🔴)·주황(🟠) 항목이 있으면 그 부분을 손봐야 합니다.

`work-plan.md`에는 시스템이 만든 **카드**들이 있어요:
- `RESEARCH-001`: "이 주장의 근거 논문 찾기"
- `WRITE-005`: "Section 3 보강 필요"

**다음 할 일**:
- 카드에 `리서치 진행해줘` 같은 명령이 적혀 있으면 그대로 입력 → ④로
- 또는 평가 약점이 보이면 flow.md 직접 수정 → 다시 분석

---

### ④ 논문 자동 검색

**명령**:
```
"리서치 진행해줘"
```

이전에 `work-plan.md`에 발급된 RESEARCH 카드들을 자동으로 실행합니다. AI가 학술 데이터베이스에서 관련 논문을 찾아옵니다.

**무엇이 생기나요?**

| 파일 | 보세요? | 무엇 |
|------|--------|------|
| `papers/consensus-results.md` | ✅ 꼭 | **검색된 논문 목록** + 각 논문 평가 |

**무엇을 확인하나요?**

`consensus-results.md`를 열면 카테고리별로 정리됨:
- 🎯 **최우선**: 핵심 논문 (꼭 필요)
- 🟢 **보조**: 보완 자료
- 🔴 **Steelman**: 본 주장에 도전하는 논문 (반박 재료)
- 🌏 **발달·횡문화**: 비교 문화 관점
- ⚙️ **방법론 비판**: 측정·방법론 관점

각 논문마다 **(a) 핵심 주장 / (b) 어디에 쓸지 / (c) 인용 강도**가 적혀 있습니다.

**다음 할 일**: ⑤ 마음에 드는 논문 PDF 다운로드

---

### ⑤ PDF 다운로드 (직접 작업)

**무엇을 하나요?**: `consensus-results.md`에서 다운로드할 논문을 정합니다. 각 논문 옆에 URL이 있어요. 클릭해서 PDF 받은 후:

```
papers/candidates/    ← 이 폴더에 PDF를 떨어뜨립니다
```

다운로드 받은 PDF는 파일 이름이 어떻든 상관없습니다 — 시스템이 알아서 이름 정리합니다.

**팁**: 한 번에 5-20편 정도 받는 게 좋아요. 너무 많으면 분석에 시간 걸립니다.

**다음 할 일**: ⑥ 논문 분석

---

### ⑥ 논문 분석

**명령**:
```
"논문 처리해줘"
```

이게 한 번에 다 해줍니다:
1. PDF 정리·중복 제거
2. 본문 추출
3. consensus-results와 매칭해서 **자동으로 중요도 분류**
4. 각 논문 분석

**무엇이 생기나요?**

| 파일 | 보세요? | 무엇 |
|------|--------|------|
| `papers/analyzed/[A].이름.md` | ✅ | **중요 논문 분석** (anchor) — 깊은 분석 (~300줄) |
| `papers/analyzed/[N].이름.md` | 선택 | 보조 논문 분석 — 간단한 분석 (~30-50줄) |

**파일명 앞 표시**:
- `[A]` = **Anchor** (가장 중요한 논문 — 본 글의 핵심 자료)
- `[N]` = **Normal** (보조 — 가볍게 인용)

**[A] 논문에 들어있는 내용**:
- **한 줄 요약**: 이 논문이 뭐라고 하는지
- **인용 가능**: 인용해서 쓸 수 있는 문장 + 페이지 번호
- **본 글에서 활용**: 어느 섹션에 어떻게 쓸지
- **다른 anchor 대비**: 다른 핵심 논문과 어떻게 다른지
- **사용자 메모**: 자유 메모 공간 (직접 추가 가능, AI 안 건드림)

**무엇을 확인하나요?**

`[A]` 논문은 한 번씩 열어보세요. 인용 가능 부분이 마음에 안 드는 것 있으면 직접 수정 가능합니다.

**팁**: 메모를 남기면 다음에 AI가 분석할 때 그 메모를 존중합니다.

**다음 할 일**: ⑦ 초안 작성

---

### ⑦ 초안 작성 (flow → output 자동 전진)

**명령**:
```
"초안 작성해줘"
```

이 명령을 실행하면 **자동으로 output 단계로 넘어갑니다**. AI가 두 단계로 진행:

1. **구조 설계**: 이런 챕터로 쓸 거다 → 사용자 승인
2. **본문 작성**: 승인된 구조대로 작성

**무엇이 생기나요?**

| 파일 | 보세요? | 무엇 |
|------|--------|------|
| `output/01-introduction.md` | ✅ | 1장 초안 |
| `output/02-...md` | ✅ | 2장 초안 |
| `output/05-conclusion.md` | ✅ | 결론 초안 |

(또는 단일 `output.md` 파일 하나에 작성될 수도 있음 — AI가 글 길이 보고 판단)

**무엇을 확인하나요?**

각 챕터를 열어보세요. 어떤 부분이:
- 약한지 (논리 구멍)
- 인용이 부족한지
- 표현이 부자연스러운지

다음 단계에서 자동으로 잡아주지만 미리 봐두면 좋아요.

**다음 할 일**: ⑧ 분석 + 수정 반복

---

### ⑧ 분석 + 수정 반복 (output 단계)

**명령** (현재 output 단계라 자동 적용):
```
"레퍼런스 분석해줘"      ← output 인용 충실도 분석
"내용 분석해줘"          ← output 논리·약점 분석
```

**무엇이 생기나요?**

`work-plan.md`에 새 **WRITE 카드**가 발급됨:
- `WRITE-007`: "Section 3 인용 보강"
- `WRITE-012`: "결론 약함 — 더 강한 입장 표명"

**카드 처리 명령**:
```
"output 03-thesis.md 수정해줘: WRITE-007"
```

(카드 ID 그대로 입력하면 그 카드대로 수정합니다.)

**자동으로 함께 일어나는 일**:
- 수정 직후 **인용 정확성 자동 검증**
- 만약 인용에 문제가 있으면 새 WRITE 카드 발급

**반복**: 분석 → 카드 처리 → 분석 → 카드 처리... 모든 카드가 처리될 때까지.

**언제 끝내나요?**: `evaluation.md`에서 모든 축이 🟢 또는 🟡일 때 (🟠·🔴 없을 때).

---

### ⑨ 마무리 단계

글이 거의 다 됐으면 마지막 다듬기 명령들:

#### 9-1. 적대적 리뷰 (선택, 강력 추천)

**명령**:
```
"적대적 리뷰 해줘"
```

특정 학파·관점에서 본 글을 공격해보는 시뮬레이션. "이런 학파라면 이렇게 비판하겠지"를 미리 알려줍니다.

→ `adversarial-review.md` 생성. 이 파일에 발견된 **구체적 반박**이 적혀 있으니 미리 막을 수 있도록 글 보강.

#### 9-2. 인용 검증

**명령**:
```
"인용 확인해줘"
```

본문의 모든 인용이 분석 결과와 일치하는지 자동 확인.

→ `output/.citation-check-report.md` 생성. 잘못된 인용·페이지 번호 mismatch·anchor 미사용 등 표시.

#### 9-3. 참고문헌 만들기

**명령**:
```
"참고문헌 만들어줘"
```

→ `output/bibliography.md` 자동 생성 (APA/MLA/Chicago 형식 선택 가능).

#### 9-4. 최종 통합

**명령**:
```
"최종 통합해줘"
```

→ `final/complete-draft.md` 생성. 모든 챕터를 하나로 합친 최종본 (.docx도 가능).

#### 9-5. 모의 심사 (선택)

**명령**:
```
"리뷰 체크해줘"
```

가상 심사자 2-3명이 본 글을 읽고 **실제 심사 같은 피드백** 줍니다.

→ `review-feedback.md` 생성.

**🎉 완성!** 이제 글을 제출하거나 인쇄하면 됩니다.

---

## 📂 폴더 구조 (사용자가 보는 것만)

```
projects/my-essay/
├── flow/
│   └── flow.md              ← ② 본인이 작성하는 줄거리
│
├── output/
│   ├── 01-introduction.md   ← ⑦ 자동 생성 본문
│   ├── 02-background.md
│   ├── ...
│   ├── 05-conclusion.md
│   └── bibliography.md      ← 9-3 참고문헌
│
├── papers/
│   ├── candidates/          ← ⑤ 본인이 PDF 떨어뜨리는 곳
│   ├── consensus-results.md ← ④ 검색된 논문 목록 (검토용)
│   └── analyzed/
│       ├── [A].논문이름.md   ← ⑥ 핵심 논문 분석 (꼭 보세요)
│       └── [N].논문이름.md   ← 보조 논문 분석
│
├── final/
│   └── complete-draft.md    ← 9-4 최종본
│
├── adversarial-review.md    ← 9-1 반박 시뮬 (선택)
├── work-plan.md             ← 할 일 카드 (매일 확인)
└── flow/evaluations/evaluation.md  ← 평가 결과 (분석 후 확인)
```

위에 안 보이는 폴더들 (`history/`, `.research-raw/`, `markdown/` 등)은 시스템이 알아서 관리합니다 — **신경 안 써도 됩니다**.

---

## 🔁 매일 시작 패턴

며칠 만에 다시 작업할 때:

```
"현재 상태"
```

이 한 명령으로:
- 어디까지 했는지
- 다음에 뭘 하면 좋은지
- 미처리 카드 몇 개 있는지

가 한눈에 나옵니다.

뭘 해야 할지 모르겠으면:
```
"작업 추천해줘"
```

→ 시스템이 진행 상황 보고 **다음 명령 + 이유** 알려줍니다.

---

## 📜 주요 명령어 요약

### 매일 쓰는 것 (5개)

| 명령 | 언제 |
|------|------|
| `"my-essay 프로젝트 만들어줘"` | 처음 시작 |
| `"레퍼런스 분석해줘"` | flow.md 또는 output 작성 후 — 부족한 논문 찾기 |
| `"내용 분석해줘"` | flow.md 또는 output 작성 후 — 논리 점검 |
| `"리서치 진행해줘"` | 분석 후 RESEARCH 카드 실행 |
| `"논문 처리해줘"` | PDF 받아서 candidates에 넣은 후 |

### 가끔 쓰는 것

| 명령 | 언제 |
|------|------|
| `"초안 작성해줘"` | flow 다 됐을 때 — output 단계로 자동 전진 |
| `"output ch3.md 수정해줘: WRITE-007"` | WRITE 카드대로 수정 |
| `"논문 재분석해줘"` | flow.md 큰 변경 후 (영향 받는 논문만 자동 재분석) |
| `"비판적으로 분석해줘 X"` | 특정 논문에 비판 섹션 추가 |
| `"적대적 리뷰 해줘"` | 글 끝 단계 — 학파별 반박 시뮬 |
| `"인용 확인해줘"` | 글 끝 단계 — 인용 정확성 |
| `"참고문헌 만들어줘"` | 글 끝 단계 — 자동 bibliography |
| `"최종 통합해줘"` | 모든 챕터 합쳐 final 만들기 |
| `"리뷰 체크해줘"` | 모의 심사 시뮬 |

### 메타 (상태 확인)

| 명령 | 무엇 |
|------|------|
| `"현재 상태"` | 진행도·다음 할 일 한눈에 |
| `"작업 추천해줘"` | 시스템이 다음 명령 추천 |
| `"버전 체크"` | 파일 sync 확인 (자동, 보통 안 봐도 됨) |

---

## 🎬 자주 쓰는 시나리오

### A. 새 프로젝트 (글 시작부터 끝까지)

```
1. "my-essay 프로젝트 만들어줘"
2. flow.md를 에디터로 직접 작성
3. "레퍼런스 분석해줘" + "내용 분석해줘"
4. "리서치 진행해줘"           ← 자동 검색
5. consensus-results.md 보고 PDF 다운로드 → papers/candidates/에 투입
6. "논문 처리해줘"              ← 자동 분류·분석
7. "초안 작성해줘"              ← output 단계로 자동 진입
8. "레퍼런스 분석해줘" + "내용 분석해줘"  (이번엔 output 대상)
9. WRITE 카드들 처리 (반복)
10. "적대적 리뷰 해줘"
11. "인용 확인해줘"
12. "참고문헌 만들어줘"
13. "최종 통합해줘"
🎉
```

### B. 며칠 만에 돌아왔을 때

```
"현재 상태"             ← 어디까지 했는지
"작업 추천해줘"          ← 다음 명령 제안
```

### C. 글이 잘 안 풀릴 때

```
"내용 분석해줘"          ← 약점 정확히 진단
flow.md 또는 output 직접 수정
"내용 분석해줘"          ← 다시 점검 (개선됐는지 확인)
```

### D. 특정 부분만 수정하고 싶을 때

work-plan.md에서 카드 ID 찾기 → 그대로 명령:
```
"output 03-thesis.md 수정해줘: WRITE-007"
```

### E. AI 분석 결과가 마음에 안 들 때

각 분석 결과 파일 (`analyzed/[A].이름.md` 또는 `flow.md`)을 직접 수정 → 다시 분석 명령. AI가 사용자 수정을 존중합니다.

---

## ❓ 자주 묻는 질문 (FAQ)

**Q. 명령어를 정확히 외워야 하나요?**
A. 아니요. `"리서치 시작해"`, `"논문 좀 찾아줘"`, `"이 글 어디가 약한지 봐줘"` 같이 자연어로 말해도 알아듣습니다.

**Q. AI가 잘못된 인용을 만들면?**
A. ⑨-2 `"인용 확인해줘"` 명령이 자동으로 잡아냅니다. 또는 사용자가 paper 분석 파일 직접 수정 가능.

**Q. flow.md를 잘 못 쓰겠어요.**
A. `flow/FLOW-TEMPLATE.md`에 가이드가 있어요. 또는 한 문단만 써놓고 `"내용 분석해줘"` → AI가 약점 알려주면 그것 기반으로 보강.

**Q. PDF가 분석 안 됩니다.**
A. 빈 PDF·손상 PDF는 자동으로 `.quarantine/` 폴더로 격리됩니다. 다른 PDF로 다시 시도.

**Q. output으로 진입했는데 flow를 더 보강하고 싶어요.**
A. 시스템은 단방향(flow → output)입니다. 정말 필요하면 `"flow에서 X를 보강하고 싶다"`라고 말해주세요. AI가 안내합니다.

**Q. 카드(work-plan)가 너무 많이 쌓여요.**
A. 정상입니다. 한꺼번에 다 처리할 필요 없어요. `"작업 추천해줘"`로 우선순위 높은 것부터.

**Q. AI가 만든 분석을 직접 수정해도 되나요?**
A. 됩니다. `papers/analyzed/[A].이름.md`를 에디터로 열고 수정하세요. 다음 분석 시 AI가 그 변경을 존중합니다.

**Q. 백업은?**
A. 시스템이 자동으로 `history/` 폴더에 직전 버전을 보관합니다. 망쳐도 복구 가능.

---

## 🆘 막혔을 때

```
"현재 상태"             ← 진행도 확인
"작업 추천해줘"          ← 다음 명령 제안
"버전 체크"             ← 파일 sync 문제 확인
```

이래도 안 되면 [README.md](./README.md) 또는 [MANUAL.md](./MANUAL.md)의 트러블슈팅 섹션 참고.

---

## 📚 더 깊이

| 문서 | 언제 |
|------|------|
| [README.md](./README.md) | 설치 방법 |
| [MANUAL.md](./MANUAL.md) | 모든 기능 상세 (시스템 잘 이해하고 싶을 때) |
| [PRINCIPLES.md](./PRINCIPLES.md) | 왜 이렇게 만들었나 (설계 철학) |

---

🎯 **핵심 5가지만 기억**:
1. `"X 프로젝트 만들어줘"` 로 시작
2. flow.md를 줄글로 작성
3. `"레퍼런스 분석해줘"` + `"내용 분석해줘"`
4. work-plan.md의 카드대로 진행
5. `"현재 상태"` 가 매일 출발점
